name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  ci:
    name: Lint · Typecheck · Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Enable Corepack
        run: corepack enable

      - uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      # Scoped to production deps: dev-only tooling (eslint/tailwind's
      # transitive advisories) is tracked separately and doesn't gate deploys.
      - name: Audit
        run: yarn npm audit --recursive --environment production --severity high

      - name: Lint
        run: yarn lint

      - name: Format check
        run: yarn prettier

      - name: Typecheck
        run: yarn typecheck

      - name: Build
        run: yarn build
